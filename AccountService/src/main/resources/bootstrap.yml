spring:
  application:
    name: AccountService

server:
  port: 8802


eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8701/eureka,http://localhost:8702/eureka